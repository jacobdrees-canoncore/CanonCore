---
status: proposed
---

# There is no `object` kind

A fictional physical object — a wand, a ring, a map — is `concept` plus a category statement.

`concept` will read wrong for the first object anyone catalogues, and it is still the answer. An
unexplained mismatch between this rule and the first real item is how enum creep starts, and the
kind list is closed.
