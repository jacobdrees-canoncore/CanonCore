-- Migration 9, under CNCORE-66. THE INDEX CATALOGUE SEARCH READS.
--
-- GENERATED by `drizzle-kit generate` from the declaration on `items` in
-- `src/schema/tables.ts`, which is why it is one line and not the essay
-- migration 8 is. The reasoning lives in the two places a reader will actually
-- be standing: beside the declaration, and in the rung that installs the
-- extension this DDL cannot run without.
--
-- IT MUST STAY BEHIND MIGRATION 8 IN THE LADDER. `gin_trgm_ops` is an operator
-- class `pg_trgm` provides, so this statement fails outright on a database that
-- has not installed it. `scripts/check-ladder.ts` is what keeps that ordering
-- true on an EXISTING database as well as a fresh one (ADR-0047).
CREATE INDEX "items_title_trigram" ON "items" USING gin ("title" gin_trgm_ops);
