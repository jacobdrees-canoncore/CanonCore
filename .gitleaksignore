# Accepted gitleaks findings, with the reason each is accepted.
#
# EMPTY, AND THAT IS THE POINT. This repository begins at a single seed commit
# (ADR-0114), so the two third-party values this file used to pin do not exist
# anywhere in it: they were redacted from the working files long ago and lived
# only in commits that this repository does not have. Their fingerprints pinned
# those commits by SHA, so carrying them here would assert that values are in a
# history that never contained them -- and one of the three named a file this
# repository does not even hold.
#
# Do not add to this file to make a check pass. If a finding is genuinely ours,
# rotate the credential -- do not record it here. If a finding is a third-party
# public value quoted as research evidence, redact the value from the working
# file and say so beside the quotation, which is what was done before.
